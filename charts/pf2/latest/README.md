Ретранслятор tcp соединений �ля работы приложения требуется предварительно установить библиотеку POCO Установка приложения �агрузите установочный пакет 
bin/pf2.deb �ыполните установку sudo dpkg -i pf2.deb �ля удаления приложения используйте sudo dpkg -r pf2 Остановка и запуск �риложение устанавливается как 
сервис. �ля запуска используйте: sudo systemctl start pf2.service �ля остановки используйте: sudo systemctl stop pf2.service �ля проверки статуса используйте: 
sudo systemctl status pf2.service Настройка �араметры конфигурации находятся в текстовом файле /opt/pf2/pf2.conf �равила обработки файла конфигурации: �трока 
начинающаяся с '#' или ';' не обрабатывается, равно как и пустые строки. Не пустая строка должна содержать ключ=значение, иное не допускается. Параметры: dump - 
отображение дампа в логе, 1 - включено, по умолчанию выключено dump=1 timeout (синоним to) - таймаут не активности в секундах, по истечении таймаута соединение 
принудительно закрывается, по умолчанию выключено timeout=30 Режимы работы: forward (синоним fwd) - проброс tcp соединения с одного порта на другой адрес и порт. 
Пример: forward=listen-port:remote-address:remote-port forward=7000:127.0.0.1:9000 retranslator (синоним ret) - ретранслятор tcp соединения (соединение двух tcp 
клиентов). Пример: retranslator=listen-port-A:listen-port-B retranslator=8001:8002 retranslator-fool (синоним retf) - ретранслятор tcp соединения (соединение 
двух tcp клиентов) "дурацкий" режим, не разрывает соединение для клиента, подключившегося к порту 'A' если неожиданно отключился клиент подключенный к порту 'B' 
и наоборот, не разрывает соединение для клиента, подключившегося к порту 'B' если неожиданно отключился клиент подключенный к порту 'A'. Таймауты в этом режиме 
не обрабатываются. Пример: retranslator-fool=listen-port-A:listen-port-B retranslator-fool=9001:9002 echo - эхо сервер, используется только для проверки 
соединения клиентами с сервером. Пример: echo=listen-port echo=9000 После выполения настроек необходимо перезапустить сервис. sudo systemctl restart pf2.service 
Журнал
� процессе выполнения приложение ведет логирование в файле /var/log/pf2.log
